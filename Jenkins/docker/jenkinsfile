pipeline {
    agent any
    stages {
        stage('Docker Build') {
            steps {
                sh 'docker build -t myapp:latest .'
            }
        }
        stage('Docker Push') {
            steps {
                sh 'docker push myrepo/myapp:latest'
            }
        }
    }
}
